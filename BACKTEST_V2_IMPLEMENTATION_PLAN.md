# 보조지표 신뢰도 감사·백테스트 성능 개선 계획

## 목표

공개 대시보드의 EMA, RSI, MACD, ATR, Bollinger Band, 동적 POC가 표준 계산과 일치하는지 검증하고, 현재 POC 신호의 비용 포함 성과를 여러 종목·시간봉에서 다시 측정한다. 개선안은 학습 구간에서만 선택하고 보지 않은 홀드아웃 구간에서 재검증해 특정 코인이나 특정 기간에만 맞는 설정을 배제한다.

## 1단계: 계산식과 데이터 신뢰도 감사

1. Binance USDT-M Futures 서버 시각을 기준으로 종료된 캔들만 사용한다.
2. 캔들 시간 중복, 시간 간격 누락, OHLC 역전, 0 이하 가격·거래량을 검사한다.
3. EMA, RSI, MACD, ATR, Bollinger Band를 대시보드 구현과 독립 기준 구현으로 계산해 허용 오차를 비교한다.
4. 각 지표의 준비 구간은 값이 없는 상태로 유지하고, 준비되지 않은 값이 신호 판정에 사용되지 않는지 검사한다.
5. Volume Profile은 체결별 원시 분포가 아닌 OHLCV 기반 근사치임을 정확도 평가와 화면 설명에 명시한다.

## 2단계: 기준 백테스트 재측정

1. BTCUSDT, ETHUSDT, BNBUSDT, XRPUSDT, SOLUSDT를 1시간·4시간·8시간·12시간·1일 봉으로 검사한다.
2. 각 조합은 최근 최대 5,000개 확정봉을 사용한다.
3. 신호 확정 다음 봉 시가에 진입하고, 진입·청산 양쪽에 수수료 0.04%와 슬리피지 0.02%를 반영한다.
4. 같은 봉에서 손절과 목표가 모두 닿으면 손절을 우선한다.
5. 거래 수, 승률, Profit Factor, 평균 순손익 R, 누적 순손익 R, 최대낙폭 R을 기록한다.

## 3단계: 과최적화 방지형 개선안 비교

1. 기존 POC 신호를 기준선으로 둔다.
2. 보조지표를 무작정 더하지 않고 다음 후보를 각각 분리해 비교한다.
   - EMA 20/50 추세 방향 필터
   - RSI 과열·침체 회피 필터
   - MACD 히스토그램 방향 필터
   - Bollinger 중심선 방향 필터
   - 거래량 활성도 필터
3. 각 데이터의 앞 60%에서 후보를 평가하고, 모든 시장에 공통으로 적용할 하나의 규칙만 선택한다.
4. 선택 규칙은 마지막 40% 홀드아웃에서 기준선보다 Profit Factor·평균 R·최대낙폭이 함께 개선되는지 확인한다.
5. 거래 수가 지나치게 줄거나 특정 한 조합에서만 좋아지면 채택하지 않는다.

## 4단계: 최소 범위 구현

1. 독립 감사 스크립트와 재현 가능한 결과 JSON/보고서를 추가한다.
2. 계산 오류가 확인된 경우에만 대시보드 지표 코드를 수정한다.
3. 홀드아웃에서 반복 개선된 필터가 있을 때만 기본 신호에 반영한다.
4. 백테스트 중 화면 멈춤을 줄이도록 계산 진행 상태와 데이터 요청 오류를 명확히 표시한다.

## 5단계: 회귀·공개 배포 검증

1. 지표 기준값, 미래 참조 방지, 다음 봉 진입, 보수적 동시 체결, 비용 계산 테스트를 통과시킨다.
2. 검색, 즐겨찾기, 시간봉 변경, 현재 상태와 과거 신호 분리, 390px 모바일 화면을 확인한다.
3. 로컬 결과와 공개 페이지가 동일한 커밋인지 확인하고, 공개 URL에서 백테스트 버튼을 직접 실행한다.

## 채택 기준

- 수식 정확도는 독립 기준 구현과 오차 허용 범위 안에서 일치해야 한다.
- 전략 개선은 홀드아웃 전체 합산 Profit Factor와 평균 R이 기준선보다 좋아야 한다.
- 홀드아웃 시장 조합의 과반에서 성과 방향이 악화되지 않아야 한다.
- 최소 150건 이상의 홀드아웃 종료 거래를 유지해야 한다.
- 위 조건을 만족하지 않으면 성능 개선을 주장하지 않고 기존 신호를 관찰 도구로 유지한다.

## 완료 조건

- 최신 확정봉 데이터 무결성 검사와 지표 수식 감사 결과가 남는다.
- 기준선과 후보별 학습/홀드아웃 성과가 재현 가능한 파일로 생성된다.
- 채택 또는 미채택 이유가 수치로 설명된다.
- 수정이 필요한 경우 테스트, 커밋, 배포, 공개 브라우저 검증까지 완료한다.
