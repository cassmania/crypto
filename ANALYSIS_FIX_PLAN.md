# 분석 로직 정정 계획

## 확인된 문제

- 화면의 `POC·VAH·VAL`은 최근 200개 확정 캔들 전체로 계산한 고정 매물대입니다.
- 화면의 추세·재확인·ATR 신호는 최근 Lookback(기본 100개) 캔들로 계산한 동적 POC를 사용합니다.
- 두 지표는 계산 범위와 역할이 다르지만, 현재 시나리오 문구가 같은 기준처럼 연결해 `VAH 재돌파`처럼 이미 지나간 가격을 다음 목표로 안내할 수 있습니다.
- 분석 판단은 확정 캔들 기준인데, 화면 상단의 실시간가와 바로 붙어 있어 사용자가 실시간가로 신호가 확정된 것처럼 오해할 수 있습니다.

## 수정 범위

1. 고정 매물대와 동적 추세 기준을 화면 문구에서 명확히 분리합니다.
2. 롱·숏 시나리오를 현재 확정 종가의 위치에 따라 계산합니다.
   - VAH 아래: POC 지지 확인 후 VAH 재시험
   - VAH 위: VAH 지지 유지 여부와 다음 고정 저항 확인
   - VAL 아래: VAL 회복 실패 여부와 하단 매물대 확인
3. 신호 상태에 `확정 캔들 기준`을 명시하고, 실시간가는 참고값으로만 표시합니다.
4. 현재가와 가장 가까운 고정 매물대를 목표/저항·지지 후보로 사용해 문구가 실제 가격 위치와 모순되지 않도록 합니다.

## 검증 방법

1. BTCUSDT 4시간봉에서 VAH 위·가치영역 내부·VAL 아래 상태별 문구를 확인합니다.
2. 확정 캔들 시각과 실시간가 시각이 구분되어 보이는지 확인합니다.
3. 신호 표의 진입·무효화·목표 값이 같은 확정 캔들 기준으로 표시되는지 확인합니다.
