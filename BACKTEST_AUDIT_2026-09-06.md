# 백테스트 재현성 및 경계 손익 개선 계획

1. 현재 코드와 보고서를 백업하고 기존 회귀 테스트를 실행한다.
2. Binance Futures 5종·5시간봉의 최신 확정봉을 고정 저장한다. 이후 동일 데이터로 수정 전후를 비교한다.
3. 데이터·학습 경계의 미청산 포지션을 마지막 종가로 청산해 손익 누락을 막는다. 공개 화면과 감사 스크립트를 함께 수정한다.
4. 시장별 거래 합산 최대낙폭을 청산 시각순으로 계산한다. 반복 배열 복사를 줄이되 결과 동등성을 검증한다.
5. 지표 공식·체결·시간 경계 회귀 테스트 및 실제 화면 검증 후 보고서와 함께 배포한다.

## 한계

시장 조합별 최근 5,000봉을 사용하므로 기간과 분할 날짜가 다르다. 합산 성과는 공통 달력 기준의 독립 워크포워드가 아니다. 이미 검토한 기간을 포함한 재검증이며, 반복 지표 표본의 적중률을 독립적인 미래 성공 확률로 표시하지 않는다. 최대낙폭은 누적 R의 청산 기준이며 계좌·레버리지·미실현 손익 낙폭이 아니다.

## 실행 결과

Binance USDT-M 5종(BTC·ETH·BNB·XRP·SOL), 5시간봉(1H·4H·8H·12H·1D), 총 111,034개 확정봉을 검사했다. 각 시장 앞 60% 학습·뒤 40% 검증, 다음 봉 시가 진입, 손절 1.5 ATR·목표 3 ATR, 편도 수수료 0.04%·슬리피지 0.02%다.

| 항목 | 같은 데이터 수정 전 | 수정 후 |
|---|---:|---:|
| 기본 전략 검증 거래 | 1,097 | 1,099 |
| 승률 | 34.28% | 34.21% |
| Profit Factor | 0.953 | 0.953 |
| 평균 순손익 | -0.033R | -0.033R |
| 누적 순손익 | -35.896R | -36.115R |
| 합산 최대낙폭 | 91.664R | 133.996R |

수익 우위는 확인되지 않았다. 최대낙폭 변화에는 종목 나열 순서에서 청산 시각순으로 계산을 바로잡은 효과가 포함되어 있다. 실제 시장 위험이 이번 수정 때문에 커졌다는 뜻은 아니다.

학습에서 선택한 RSI 필터 후보는 검증 943거래, PF 0.973, 평균 -0.019R로 여전히 음수다. 개선 조합도 25개 중 12개로 과반 기준에 미달해 기본 신호에 적용하지 않았다. 채택 기준에는 비용 반영 양의 기대값과 PF>1을 명시적으로 추가했다.

## 수정 내용과 검증

1. 열린 포지션: 기존에는 표본에서 빠졌던 잔여 거래를 구간 마지막 종가로 청산하고 비용을 차감한다. 해당 포지션 보유 중 다른 신호로 중복 진입하지 않는다.
2. 최대낙폭: 여러 시장의 거래를 실제 청산 시각순으로 정렬한 뒤 누적 R의 고점 대비 하락을 구한다. 종목 배열 순서를 바꿔도 결과가 같은지 테스트했다.
3. 성능: 거래마다 학습 배열 전체를 복사하지 않고 종료 인덱스를 전달한다. 잘라낸 배열과 종료 인덱스를 전달한 계산의 동등성을 테스트했다. 전체 화면 속도 개선율은 측정하지 않았다.
4. 재현성: 원자료를 로컬 audit-snapshot.json에 저장하고 --replay로 동일 자료를 재실행한다. 입력 해시는 backtest-v2-results.json에 기록한다. 데이터 갱신 실행 시 파일이 바뀌므로 이전 비교가 필요하면 별도 보관한다.
5. 브라우저 테스트 9개와 별도 감사 무결성 테스트를 통과했다. 지표 수식, 갭 손절, 비용, 경계 청산, 360/390px 모바일, 1920px 배치를 포함한다.

## 재현 명령

`node scripts/backtest-v2-audit.js`는 최신 스냅샷을 수집한다. `node scripts/backtest-v2-audit.js --replay`는 저장된 자료를 재검증한다. `node tests/audit-integrity.cjs`는 경계와 시간순 집계를 검사한다. `npm test`는 8787 포트의 로컬 서버에서 브라우저 테스트를 실행한다.
