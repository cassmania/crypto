# 동적 POC 지표 백테스트·정확도 개선 계획

## 목표

현재 배포된 동적 POC 관찰 신호를 실제 Binance USDT-M 확정봉으로 재현하고, 비용을 포함한 홀드아웃 백테스트로 유효성을 판단한다. 계산 오류가 확인된 지표만 최소 범위로 수정하고, 검증 결과를 대시보드에서 과장 없이 확인할 수 있게 한다.

## 현재 확인한 위험

1. RSI 첫 14개 상승·하락 합계를 평균으로 바꾸지 않은 채 다음 Wilder 갱신에 사용하고 있어, 15번째 값부터 최신 변화가 지나치게 작게 반영될 수 있다.
2. ATR이 일반적으로 쓰는 Wilder RMA가 아니라 EMA로 계산되어 손절·목표·밴드와 백테스트 결과가 표준 ATR과 달라질 수 있다.
3. 현재 API 요청은 최근 확정봉 200개뿐이라, 최대 200봉 Lookback과 평활·ATR 워밍업을 함께 쓰면 초반 신호가 충분히 안정화되지 않는다.
4. 현재 화면에는 신호의 진입 참고가만 있고, 이후 목표·무효화 도달 여부와 비용 포함 성과가 없어 정확도를 확인할 수 없다.

## 구현 범위

1. RSI를 표준 Wilder 방식으로 수정하고 알려진 입력값 테스트로 검증한다.
2. ATR을 Wilder RMA로 수정하고 첫 유효값 이전은 계산 준비 상태로 처리한다.
3. 백테스트를 실행할 때만 Binance Kline을 페이지네이션해 최대 5,000개 확정봉을 별도로 가져오고, 화면 차트는 기존 최근 200봉 흐름을 유지한다.
4. 기존 동적 POC 신호 규칙을 다음 봉 시가 진입으로 고정해 거래를 재생한다.
5. 수수료·슬리피지를 진입과 청산 양쪽에 반영하고, 같은 봉에서 목표와 손절이 모두 닿으면 손절을 우선한다.
6. 시간 순서의 앞 60%는 워밍업·개발 구간, 마지막 40%는 홀드아웃으로 분리해 홀드아웃 성과를 우선 표시한다.
7. 거래 수, 승률, Profit Factor, 평균 순손익 R, 최대낙폭 R을 표시하고 30거래 미만은 `표본 부족`으로 명시한다.
8. 성과가 나쁜 설정을 자동으로 좋은 전략이라고 표현하지 않고 `우위 확인`, `혼합`, `전략 우위 미확인`, `표본 부족`으로 판정한다.

## 검증 기준

1. 지표 단위 테스트: RSI와 ATR이 독립 기준 계산과 허용 오차 안에서 일치한다.
2. 미래 참조 방지: 신호는 현재 확정봉까지만 사용하고 진입은 반드시 다음 봉 시가다.
3. 체결 보수성: 한 봉에서 손절·목표가 동시에 닿으면 손절 처리한다.
4. 데이터 완결성: 진행 중인 캔들은 제외하고, 거래소·심볼·시간봉·조회 기간·마지막 확정봉을 결과에 표시한다.
5. 실데이터 검증: BTCUSDT, ETHUSDT, SOLUSDT의 1시간·4시간봉에서 동일 비용 조건으로 실행한다.
6. 회귀 검증: 검색, 즐겨찾기, 시간봉 전환, 현재 상태/과거 신호 분리, 모바일 오버플로가 기존처럼 동작한다.

## 완료 조건

- 계산 오류 수정과 테스트가 통과한다.
- 실제 Binance 확정봉 백테스트 결과 파일이 생성된다.
- 대시보드에서 백테스트 조건과 홀드아웃 핵심 성과를 확인할 수 있다.
- 공개 배포본이 로컬 커밋과 일치하고 브라우저에서 정상 동작한다.
