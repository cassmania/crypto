# 화면 채움·라이트 모드·지지저항 정밀화 구현 계획

## 목표

PC의 넓은 모니터와 스마트폰에서 불필요한 왼쪽 공백 없이 화면 폭을 활용하고, 전체 화면과 차트를 밝은 라이트 모드로 통일한다. 지지·저항은 단일 스윙 한 번에 의존하지 않고 매물대·다중 스윙·거래량·반복 접촉·최근성을 함께 평가해 더 촘촘하고 읽기 쉽게 표시한다.

## 1단계: 반응형 배치 개선

1. 앱의 1,440px 최대 폭 제한을 제거하고 화면 전체 폭을 사용한다.
2. 차트는 첫 줄 전체 폭에 배치한다.
3. 지표·설정·백테스트·매물대·지지저항·시나리오 카드는 PC에서 3열, 태블릿에서 2열, 스마트폰에서 1열로 자동 배치한다.
4. 최근 신호 표는 정보량이 많으므로 PC에서 전체 폭을 사용한다.
5. 차트 높이는 화면 높이에 따라 자동 조절하되 스마트폰에서 과도하게 길어지지 않게 제한한다.
6. 360px, 390px, 768px, 1440px, 1920px 화면에서 가로 넘침과 큰 빈 영역을 검사한다.

## 2단계: 라이트 모드 전환

1. 배경, 카드, 입력창, 버튼, 표, 상태 박스를 밝은 색으로 변경한다.
2. 본문과 보조 문구는 흰 배경에서 충분히 구분되는 진한 색으로 조정한다.
3. 차트 배경·격자·축·십자선도 라이트 모드로 통일한다.
4. 저항은 빨강 계열, 지지는 초록 계열, POC는 황금색으로 일관되게 표시한다.

## 3단계: 지지·저항 정밀화

1. 기존 지역 고거래량 매물대에 반경 2봉 단기 스윙과 반경 5봉 주요 스윙을 함께 사용한다.
2. 스윙 가격은 거래량과 최근성으로 가중한다.
3. 서로 가까운 후보를 기존 0.30 ATR보다 좁은 0.14 ATR 기준으로 병합해 서로 다른 가격대가 과도하게 합쳐지는 문제를 줄인다.
4. 각 병합 가격대의 반복 접촉 횟수와 최근 접촉을 계산해 강도 점수를 만든다.
5. 현재가보다 위는 저항, 아래는 지지로 다시 분류하고 거리순으로 각각 최대 3개만 표시한다.
6. 카드에는 중심 가격뿐 아니라 가격 구간, 현재가와의 ATR 거리, 강도 점수, 근거를 표시한다.
7. 차트 축에는 R1~R3, S1~S3처럼 짧은 이름을 사용해 라벨 겹침을 줄인다.

## 4단계: 검증

1. 모든 저항이 현재가 위, 모든 지지가 현재가 아래인지 단위 테스트한다.
2. 지지·저항이 현재가에서 가까운 순서인지 검사한다.
3. EMA·RSI·MACD·ATR·Bollinger·백테스트 기존 테스트가 그대로 통과하는지 확인한다.
4. 실제 Binance 확정봉을 불러와 차트와 카드가 정상 표시되는지 확인한다.
5. PC와 모바일 렌더링을 캡처해 화면 폭 사용률과 가로 넘침을 확인한다.

## 완료 조건

- PC에서 차트 아래 왼쪽에 큰 공백이 남지 않는다.
- 스마트폰에서 모든 카드가 한 열로 화면 폭에 맞게 표시된다.
- 전체 UI와 차트가 라이트 모드로 일관된다.
- 지지·저항 카드에 근거·구간·거리·강도가 표시된다.
- 자동 테스트와 실제 공개 페이지 검증이 통과한다.
