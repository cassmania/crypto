{
  "name": "crypto-volume-profile-dashboard",
  "private": true,
  "scripts": {
    "test": "playwright test tests/dashboard.spec.js --reporter=line --workers=1",
    "validate:markets": "node scripts/run-validation.js"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
