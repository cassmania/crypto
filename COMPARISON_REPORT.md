# 제미나이 대시보드 비교 보고서

## 결론

제미나이 원본은 동적 POC, ATR 밴드, 신호 기록이라는 좋은 아이디어를 담고 있지만 실제 시장 분석 도구로 사용하기에는 데이터 신뢰성 문제가 큽니다. 최종 통합본은 기존 구현의 확정 캔들·실시간 티커·종목 검증·검색·호버 즐겨찾기를 기반으로, 제미나이의 동적 분석 기능만 검증 가능한 형태로 다시 구현했습니다.

## 발견한 주요 오류와 위험

1. **API 실패를 가상 시세로 숨김**
   - 원본 428~446행은 Binance 요청 실패 시 `Math.random()`으로 200개 캔들을 생성합니다.
   - 사용자는 실제 데이터와 가상 데이터를 즉시 구별하기 어렵고, POC·신호·손절·목표가가 모두 허구가 될 수 있습니다.
   - 통합본은 가상 데이터를 만들지 않고 오류 원인과 재시도 가능한 상태를 표시합니다.

2. **미확정 캔들 포함 가능성**
   - 원본 410행은 200개 Kline을 그대로 사용해 마지막 진행 중 캔들이 계산에 들어갈 수 있습니다.
   - 통합본은 201개를 요청하고 Kline 종료 시각이 현재보다 과거인 행만 남긴 뒤 최근 200개를 계산합니다.

3. **현재가와 24시간 등락률 부정확**
   - 원본은 마지막 캔들 종가를 현재가로 쓰고, 604행에서 200개 구간 첫 시가를 기준으로 등락률을 계산합니다. 선택한 시간봉에 따라 24시간이 아닌 수일 또는 수십 일 변화가 됩니다.
   - 통합본은 Binance Futures `ticker/24hr`의 `lastPrice`와 `priceChangePercent`를 별도로 사용합니다.

4. **검색 심볼 검증 부족**
   - 원본은 입력 문자열에 `USDT`를 붙여 바로 요청하므로 존재하지 않거나 거래 중지된 종목도 실행 단계에서야 실패합니다.
   - 통합본은 USDT 무기한 선물 중 현재 거래 중인 심볼 목록을 먼저 받아 정확 검색과 제안 목록에 사용합니다.

5. **과도하게 확정적인 문구**
   - 원본 620행과 680행은 단순 조건 충족을 `전환 완료`, `신호 확정`으로 표시합니다.
   - 통합본은 `관찰 신호`, `목표 참고`, `무효화`로 표현해 지표가 미래 성과를 보장하는 것처럼 보이지 않게 했습니다.

6. **요청 경합과 저장 데이터 예외**
   - 빠르게 여러 종목을 선택하면 늦게 도착한 이전 응답이 현재 화면을 덮을 수 있습니다.
   - 통합본은 요청 번호를 비교해 최신 요청만 렌더링하고, 손상된 즐겨찾기 JSON도 안전하게 복구합니다.

7. **프런트엔드 품질**
   - 원본은 운영 환경에서 권장되지 않는 Tailwind CDN을 사용하며 브라우저 경고가 발생합니다.
   - 축소 모드의 Impeccable 검사에서도 회색 글자 대비와 평평한 글자 계층 경고가 확인됐습니다.
   - 통합본은 외부 UI 프레임워크 없이 단일 HTML로 실행되며 동일 검사에서 발견 항목이 없었습니다. 단, 검사 파서가 축소 모드여서 결과는 보조 근거입니다.

## 최종 통합 기능

- Binance USDT-M 무기한 선물 실데이터
- 확정 캔들 200개 기반 EMA 20/50, RSI 14, MACD, ATR 14, Bollinger Band
- 전체 매물대 POC, VAH, VAL, HVN/LVN 지지·저항 후보
- 롤링 동적 POC와 EMA 10 평활 POC
- 평활 POC 기준 ATR 상단·하단 밴드
- 최근 POC 밴드 LONG/SHORT 관찰 신호와 무효화·목표 참고값
- 정확 코인 검색, 검색 제안, Enter 실행
- 로컬 저장 즐겨찾기와 아래 방향 호버 메뉴
- 이전 종목·시간봉 복원
- API 오류 시 가짜 데이터 금지
- 모바일 반응형 화면

## 검증 결과

- JavaScript 구문 검사 통과
- Binance 실제 응답으로 확정 캔들 200개 표시 확인
- ETH 검색·선택·즐겨찾기·호버 펼침 자동 테스트 통과
- 최근 관찰 신호 표 렌더링 확인
- 390 x 844 모바일 가로 넘침 없음
- Impeccable 정적 검사 발견 0건(축소 파서 모드)

## 차트 기능 확장

- 1시간, 4시간, 8시간, 12시간, 24시간, 주간, 월간 확정 캔들 지원
- 확대·축소 버튼은 최신 캔들을 고정하고 표시 개수만 변경
- 마우스 휠은 커서 위치를 기준으로 확대·축소
- 포인터 드래그로 과거 차트 구간 이동
- 현재가 근처 매물대 기준 1~3차 지지선·저항선과 가격 라벨 표시
- POC·ATR 밴드 돌파 조건의 LONG·SHORT 타점을 캔들에 표시
- 월봉은 상장 이후 확보된 확정 캔들이 50개 이상이면 실제 개수로 계산
- `크게 보기`에서 분석 패널을 숨기고 차트를 화면 전체 폭과 최대 74vh 높이로 확장
- 최소 12개 캔들까지 확대해 봉과 LONG·SHORT 타점을 크게 표시
- 1~3차 저항선을 빨강·주황·노랑, 1~3차 지지선을 초록·파랑·보라로 구분
- 차트 엔진이 표시 크기와 기기 픽셀 비율을 반영해 대형 화면 선명도 유지

## 참고 사이트 방식으로 차트 엔진 전환

- `crypto-futures-simulator`에서 사용한 Lightweight Charts 3.8 구조를 적용
- 캔들 시리즈와 EMA·Bollinger·평활 POC·ATR 밴드 7개 보조 시리즈 구성
- `createPriceLine()` 가격축 라벨로 POC·VAH·VAL과 1~3차 지지·저항 표시
- `setMarkers()`를 이용해 LONG·SHORT 관찰 타점을 봉 위아래에 표시
- 라이브러리 내장 휠 확대·축소, 마우스 드래그 이동, 십자선, 시간축 사용
- `ResizeObserver`로 기본 보기·크게 보기·모바일 크기 변화를 차트에 즉시 반영
- 브라우저 검증에서 가격선 9개, 보조 시리즈 7개, 타점 마커, 확대·드래그 및 콘솔 오류 0건 확인
