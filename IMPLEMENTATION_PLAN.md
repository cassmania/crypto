# 대시보드 비교 및 통합 구현 계획

## 목표

제미나이의 `dashboard.html`과 기존 `coin-volume-dashboard.html`을 비교해, 실제 Binance 데이터의 신뢰성을 유지하면서 동적 POC 분석 기능을 강화한 단일 HTML 대시보드를 만든다.

## 비교 결과

### 유지할 기존 구현

- Binance USDT 무기한 선물 종목 목록을 조회해 실제 존재하는 심볼만 선택
- 201개 캔들을 받은 뒤 진행 중인 캔들을 제외하고 최근 확정 200개만 계산
- 현재가는 24시간 티커에서 별도로 조회
- 정확 검색 즉시 분석, 부분 검색 결과 선택, 키보드 Enter 지원
- 즐겨찾기 로컬 저장 및 `즐겨찾기`에 마우스를 올렸을 때 아래로 펼쳐지는 메뉴
- API 실패 시 가짜 가격을 표시하지 않고 복구 가능한 오류 안내
- 데스크톱과 모바일 반응형 레이아웃

### 제미나이 구현에서 선별할 기능

- 롤링 구간의 동적 POC와 EMA 평활 POC
- ATR 기반 상단·하단 밴드
- POC 돌파 기반 LONG/SHORT 관찰 신호
- 현재 상태에 따른 동적 지지·저항 가격
- 최근 신호 기록 표

### 제외하거나 수정할 부분

- API 실패 시 `Math.random()`으로 가상 캔들을 만드는 기능 제거
- 미확정 캔들을 지표에 포함하는 문제 제거
- 종목 유효성 검증 없이 문자열만 붙이는 검색 방식 제거
- 24시간 등락률을 200개 캔들의 첫 시가로 대신 계산하는 방식 제거
- 확정되지 않은 상태를 `전환 완료`, `신호 확정`으로 단정하는 문구를 관찰·참고 표현으로 수정
- 외부 CDN 의존성이 큰 차트 대신 단일 HTML 내부 Canvas 차트 유지

## 구현 순서

1. 기존 신뢰성 높은 데이터 수집·검색·즐겨찾기 흐름을 유지한다.
2. 동적 POC, 평활 POC, ATR 밴드, 지지·저항, 관찰 신호 계산을 추가한다.
3. 차트와 상태 패널, 신호 기록 표를 통합한다.
4. API 오류, 빈 검색, 저장 데이터 손상 등 예외 상태를 보강한다.
5. 데스크톱·모바일에서 검색, 즐겨찾기 호버, 종목 전환, 시간봉 변경을 자동 검증한다.

## 결과 파일

- `coin-volume-dashboard.html`: 최종 실행 파일
- `COMPARISON_REPORT.md`: 오류와 선택 근거를 정리한 비교 보고서

## 차트 상호작용 확장

1. 시간봉에 1시간, 4시간, 8시간, 12시간, 24시간, 주간, 월간을 제공한다.
2. 마우스 휠과 확대·축소 버튼으로 표시 캔들 수를 조절한다.
3. 차트를 좌우로 드래그해 과거 구간을 이동하고 초기화 버튼으로 최신 구간에 복귀한다.
4. POC·VAH·VAL과 가까운 매물대에서 지지선·저항선을 계산해 차트 우측에 가격 라벨로 표시한다.
5. 확정 캔들에서 발생한 POC·ATR 밴드 돌파 조건을 LONG·SHORT 타점으로 차트에 표시한다.
6. 시간봉 전환, 확대·축소, 드래그, 모바일 오버플로를 브라우저에서 검증한다.
